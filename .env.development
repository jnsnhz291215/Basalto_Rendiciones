# Solo variables VITE_* (esto es un frontend Vue, no un servidor Express).
# Apunta al API centralizado de proyecto_basalto (el que tiene el login JWT).
VITE_API_BASE_URL=https://turnos.basalto.app
VITE_LOGIN_URL=https://inicio.basalto.app/login
VITE_APP_NAME=Basalto Rendiciones
